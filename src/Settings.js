import React from 'react';
import { View, Text } from 'react-native';

const Settings = () => {
  return (
    <View>
      <Text>Settings Screen</Text>
      {/* 설정 화면에 필요한 내용을 추가하세요 */}
    </View>
  );
};

export default Settings;